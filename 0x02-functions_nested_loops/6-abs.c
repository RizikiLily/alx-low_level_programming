#include "main.h"

/**
 * _abs - computes absolute value of a number
 * @c: number to be computed
 * Return: the abs value
 */

int _abs(int c)
{
	int abs_val
	if (c < 0)
	{
		abs_val = c * -1;
		return (abs_val);
	}
		return (c);
}
